var chai = require('chai');
var app = require('../app');
var should = chai.should();

describe('game', function() {
  it('should detect when a game is over');
  it('should allow players to ');
  it('should draw a board');
  it('should let players submit a move');
  it('should whose turn it is');
});
